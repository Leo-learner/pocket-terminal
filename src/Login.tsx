import {useState} from 'react';
import {TerminalSquare,ArrowRight} from 'lucide-react';
import {api,ApiError} from './api';
export function Login({onLogin}:{onLogin:()=>void}){const [password,setPassword]=useState(''),[busy,setBusy]=useState(false),[error,setError]=useState('');return <main className="login"><div className="login-symbol"><TerminalSquare size={34}/></div><h1>你的 Mac，<br/>随时接着用。</h1><p>登录后连接 MacBook Air 的终端。<br/>离开网页，会话继续保留。</p><form onSubmit={async e=>{e.preventDefault();if(busy)return;setBusy(true);setError('');try{await api('/login','POST',{password});setPassword('');onLogin();}catch(e){setError(e instanceof ApiError&&e.status===401?'登录密钥不正确。':e instanceof ApiError&&e.status===429?'尝试次数过多，请稍后再试。':'暂时无法连接 Mac，请确认电脑在线。');}finally{setBusy(false);}}}><input hidden name="username" autoComplete="username" value="leo" readOnly/><label htmlFor="login-key">登录密钥</label><input id="login-key" name="password" type="password" value={password} onChange={e=>setPassword(e.target.value)} autoComplete="current-password" autoCapitalize="none" spellCheck={false} required placeholder="输入你的私人密钥"/><button className="primary" disabled={busy||!password}>{busy?'正在连接…':'连接终端'}<ArrowRight size={20}/></button><p className="form-error" role="alert">{error}</p></form><small>Safari 分享 → 添加到主屏幕<br/>以独立窗口打开，获得完整终端空间。</small></main>}
