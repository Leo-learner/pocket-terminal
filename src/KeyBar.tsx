import {ArrowDown,ArrowLeft,ArrowRight,ArrowUp,Settings2,Keyboard} from 'lucide-react';
type Props={disabled:boolean;ctrl:boolean;alt:boolean;toggleCtrl:()=>void;toggleAlt:()=>void;send:(data:string)=>void;compose:()=>void;paste:()=>void;copy:()=>void;settings:()=>void;focus:()=>void};
export function KeyBar(p:Props){const key=(label:React.ReactNode,data:string,name?:string)=><button disabled={p.disabled} aria-label={name} onPointerDown={e=>e.preventDefault()} onClick={()=>p.send(data)}>{label}</button>;return <footer className="keybar"><div className="utility-row"><button onClick={p.compose} disabled={p.disabled}>输入</button><button onClick={p.paste} disabled={p.disabled}>粘贴</button><div className="spacer"/><button aria-label="打开键盘" onClick={p.focus} disabled={p.disabled}><Keyboard size={20}/></button><button onClick={p.copy}>复制</button><button aria-label="设置" onClick={p.settings}><Settings2 size={20}/></button></div><div className="key-row first">{key('Esc','\x1b','Escape')}{key('Tab','\t','Tab')}<button disabled={p.disabled} aria-pressed={p.ctrl} className={p.ctrl?'latched':''} onPointerDown={e=>e.preventDefault()} onClick={p.toggleCtrl}>Ctrl</button><button disabled={p.disabled} aria-pressed={p.alt} className={p.alt?'latched':''} onPointerDown={e=>e.preventDefault()} onClick={p.toggleAlt}>Alt</button>{key(<ArrowUp size={19}/>,'\x1b[A','上方向键')}</div><div className="key-row second">{key('Ctrl+C','\x03','中断 Ctrl+C')}{key('/','/')}{key('-','-')}{key(<ArrowLeft size={19}/>,'\x1b[D','左方向键')}{key(<ArrowDown size={19}/>,'\x1b[B','下方向键')}{key(<ArrowRight size={19}/>,'\x1b[C','右方向键')}</div></footer>}
